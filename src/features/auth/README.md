# Auth Feature

인증 관련 기능을 구현하는 디렉토리입니다.

## 구조

```
auth/
├── AuthPage.tsx        # 인증 페이지 컴포넌트
├── auth.api.ts         # 인증 API 함수들
├── auth.store.ts       # Zustand store (인증 상태 관리)
├── components/         # 인증 관련 컴포넌트
│   └── ...
└── index.ts           # Public exports
```
